# ═══════════════════════════════════════════════════════════════
# PaperPort Backend — Environment Variables
# ═══════════════════════════════════════════════════════════════

# Database — Supabase PostgreSQL (production) or SQLite (local dev)
# Supabase: Go to Project Settings > Database > Connection string > URI
# Use the "Transaction" pooler URL (port 6543) for DATABASE_URL
# Use the "Session" pooler URL (port 5432) for DIRECT_URL
DATABASE_URL="postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres"

# For local development with SQLite (change provider in schema.prisma to "sqlite"):
# DATABASE_URL="file:./dev.db"
# DIRECT_URL=""

# Redis (optional — app works without it; use Upstash for free production Redis)
REDIS_URL=""

# Gemini API Key (primary) — https://aistudio.google.com/apikey
GEMINI_API_KEY=""

# OpenAI API Key (fallback) — https://platform.openai.com/api-keys
OPENAI_API_KEY=""

# ICICI Breeze API — https://api.icicidirect.com
BREEZE_API_KEY=""
BREEZE_SECRET_KEY=""
BREEZE_SESSION_TOKEN=""

# JWT signing secret (generate: openssl rand -base64 32)
JWT_SECRET=""
JWT_ALGORITHM="HS256"
JWT_EXPIRES_IN="24h"

# Encryption key for stored credentials (generate: openssl rand -base64 32)
ENCRYPTION_KEY=""

# CORS origins — your Netlify URL + localhost for dev
# Example: "https://paperport.netlify.app,http://localhost:5173"
CORS_ORIGINS="http://localhost:5173"

# Server
HOST="0.0.0.0"
PORT=8000
NODE_ENV="development"
