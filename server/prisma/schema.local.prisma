// Local development schema — uses SQLite
// To use: copy this file over schema.prisma for local dev
// cp schema.local.prisma schema.prisma

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}
